9강.함수형 프로그래밍
자바에는 함수가 없기에(OOP라서) 1.8 부터는 자바도 가능해짐
함수는 인자, 반환, 객체 3가지 조건을 갖춰야 한다.
클래스란 객체를 만들기 위한 '설계도'이다

Lamda 의 특징
1. 고계 함수 (높은 수준의 계체)
2. 순수함수  (전역 변수를 참조하지x)
함수의 실행이 외부의 값을 변경하지 않음
3. 익명함수  (이름은 필요없고, 인자(파라미터의 값으로 구분함)



점프 투 자바
(자바에 대한 기초를 알려줌)

char 타입 같은 경우 한자리 값만 입력 받을때는 exception 관련에
이용하기 좋음 DB까지 안가도 됨

StringBuffer!

데이터 값을 정밀하게 맞춰 다룰려고 할 때
Array를 쓴다.(고정)
List는 가변배열, index가 있고(key역할을 함)
map에는 index가 없다.

map대신 voDTO를 쓰는 이유??
=> LIST<voDTO> 쓰는 이유 < >   <=제네릭스 (타입을 정해줌) 순서 있음

Map은 상세 개념임. 순서 없음
for if문으로 (범위형이니까) 값이 정해져 있는 값이라면 이걸 쓰지 말고. 
실행될 때 범위가 정해질 때
switch/case 사용하기 default도 있으니까.
while : 변동형 반복이 있을때 (최대값이 바뀔때 ex 통신, 파일)
for 알고리즘에 사용
메소드는 클래스에 종속
함수는 기능 그 자체 종속안됭.
유니크함(object는 실제 물질) 모호하고 추상적인것(클래스=설계도)
enum????



Lamda와 스트림 예제 해보기
tiles 연습해보기


리눅스
grade, account


object는 실제 물질
